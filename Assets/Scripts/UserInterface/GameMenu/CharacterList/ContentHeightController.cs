using UnityEngine;

namespace UserInterface.GameMenu.CharacterList
{
    public class ContentHeightController //: MonoBehaviour
    {
        /*void Update()
        {
            var transforms = GetComponentsInChildren<Transform>();
            int totalTransformHeightValue;
            foreach (var transform in transforms)
            {
                totalTransformHeightValue += transform.transform.
            }
        }*/
    }
}